# Experiment Backlog — Full Worked Examples

Skill: `experiment-backlog` | Load when producing output for this workflow.

## Example 1 — Typical invocation

**Input:** "Run `experiment-backlog` for [concrete task]"

**Output:**
```
Invoked `experiment-backlog`.
Step 1: Gather Candidates
Step 2: Tag Each Candidate
Step 3: Score with ICE + Feasibility
See SKILL.md Impact Report schema.
```

## Example 2 — Success criteria

**Input:** "Use `experiment-backlog` on this project"

**Output:**
```
See SKILL.md Impact Report schema.
```

---

See `SKILL.md` for hard rules, gotchas, and verification checklist.
